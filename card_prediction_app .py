# -*- coding: utf-8 -*-
"""Card Prediction App

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vD2wPCpaFmNxNJniwVthDLandUFq5X_6
"""

import streamlit as st
import pandas as pd
import numpy as np
import time
import plotly.graph_objects as go
import plotly.express as px
from sklearn.ensemble import RandomForestClassifier
from sklearn.preprocessing import LabelEncoder

# -----------------------------------------------------------------------------
# 1. PAGE SETUP
# -----------------------------------------------------------------------------
st.set_page_config(
    page_title="Card Category Intelligence",
    page_icon="💳",
    layout="wide"
)

# Custom CSS for Dark Mode "Ace3" Aesthetic
st.markdown("""
<style>
    /* Main Background - Dark Streamlit Default */
    .stApp {
        background-color: #0E1117;
        color: #FAFAFA;
    }

    /* Main Card Styling - Dark Surface */
    .metric-container {
        background-color: #262730;
        padding: 30px;
        border-radius: 15px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.5); /* Stronger shadow for depth */
        text-align: center;
        border: 1px solid #41444C;
        margin-bottom: 20px;
    }

    /* Typography */
    .big-stat {
        font-size: 3rem;
        font-weight: 800;
        margin: 10px 0;
        line-height: 1.2;
        color: #FFFFFF;
    }
    .stat-label {
        font-size: 1.1rem;
        color: #B0B0B0; /* Light Grey for labels */
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1.5px;
    }

    /* Section Headers - Force White */
    h1, h2, h3, h4, h5, h6 {
        color: #FAFAFA !important;
        font-weight: 700;
    }

    /* Highlight Box for Insights */
    .highlight-box {
        background-color: #1A1C24;
        color: #FAFAFA;
        padding: 20px;
        border-radius: 10px;
        border-left: 5px solid #2196f3;
        box-shadow: 0 2px 5px rgba(0,0,0,0.3);
        margin-bottom: 15px;
    }

    /* Sidebar Text Fix */
    .css-1d391kg {
        color: #FAFAFA;
    }
</style>
""", unsafe_allow_html=True)

# -----------------------------------------------------------------------------
# 2. INTELLIGENT DATA LOADER
# -----------------------------------------------------------------------------

@st.cache_data
def load_and_train_model(uploaded_file):
    """
    Reads the specific structure of DataSet.csv, trains a model,
    and calculates segment averages for the 'Why' analysis.
    """
    # Load Data
    df = pd.read_csv(uploaded_file)

    # --- 1. CLEANING & ENCODING ---
    keep_cols = [
        'Customer_Age', 'Dependent_count', 'Education_Level', 'Income_Category',
        'Credit_Limit', 'Total_Revolving_Bal', 'Total_Trans_Amt',
        'Total_Trans_Ct', 'Avg_Utilization_Ratio', 'Card_Category'
    ]

    # Filter only existing columns
    df = df[[c for c in keep_cols if c in df.columns]]

    # Ordinal Mapping for Income
    income_map = {
        'Unknown': 0, 'Less than $40K': 1, '$40K - $60K': 2,
        '$60K - $80K': 3, '$80K - $120K': 4, '$120K +': 5
    }
    if 'Income_Category' in df.columns:
        df['Income_Encoded'] = df['Income_Category'].map(income_map).fillna(0)

    # Label Encoding for categorical text
    le_dict = {}
    cat_cols = ['Education_Level']
    for col in cat_cols:
        if col in df.columns:
            le = LabelEncoder()
            df[col + '_Code'] = le.fit_transform(df[col].astype(str))
            le_dict[col] = le

    # Define Features (X) and Target (y)
    feature_cols = [
        'Customer_Age', 'Dependent_count', 'Credit_Limit', 'Total_Revolving_Bal',
        'Total_Trans_Amt', 'Total_Trans_Ct', 'Avg_Utilization_Ratio',
        'Income_Encoded', 'Education_Level_Code'
    ]

    # Drop rows with missing values
    df_clean = df.dropna(subset=feature_cols + ['Card_Category'])

    X = df_clean[feature_cols]
    y = df_clean['Card_Category']

    # --- 2. TRAINING ---
    clf = RandomForestClassifier(n_estimators=100, max_depth=10, random_state=42)
    clf.fit(X, y)

    # --- 3. SEGMENT PROFILING ---
    segment_profiles = df_clean.groupby('Card_Category')[feature_cols].mean()

    return clf, feature_cols, segment_profiles, le_dict, df_clean

# -----------------------------------------------------------------------------
# 3. SIDEBAR: CUSTOMER INPUT
# -----------------------------------------------------------------------------
st.sidebar.header("Customer Profile")

# File Uploader
uploaded_file = st.sidebar.file_uploader("Upload DataSet.csv", type=['csv'])

if not uploaded_file:
    st.info("Please upload the `DataSet.csv` file to initialize the AI.")
    st.stop()

# Train/Load (Cached)
model, feature_names, profiles, encoders, raw_df = load_and_train_model(uploaded_file)
st.sidebar.success("Model Trained on Uploaded Data!")
st.sidebar.markdown("---")

# Input Form
with st.sidebar.form("input_form"):
    st.subheader("Demographics")
    age = st.number_input("Age", 20, 80, 45)
    dependents = st.slider("Dependents", 0, 5, 2)
    education = st.selectbox("Education", ["Uneducated", "High School", "College", "Graduate", "Post-Graduate", "Doctorate", "Unknown"])
    income = st.select_slider("Income Range", options=['Unknown', 'Less than $40K', '$40K - $60K', '$60K - $80K', '$80K - $120K', '$120K +'], value='$60K - $80K')

    st.subheader("Financials")
    credit_limit = st.number_input("Credit Limit ($)", 1000, 40000, 8500)
    revolving_bal = st.number_input("Revolving Balance ($)", 0, 3000, 1000)
    utilization = st.slider("Utilization Ratio", 0.0, 1.0, 0.15)

    st.subheader("Behaviors (Last 12m)")
    trans_amt = st.number_input("Total Trans Amt ($)", 0, 20000, 4000)
    trans_ct = st.number_input("Total Trans Count", 0, 150, 70)

    submit = st.form_submit_button("Predict Category")

# -----------------------------------------------------------------------------
# 4. PREDICTION LOGIC
# -----------------------------------------------------------------------------
if submit:
    # Prepare Input Vector
    income_map = {'Unknown': 0, 'Less than $40K': 1, '$40K - $60K': 2, '$60K - $80K': 3, '$80K - $120K': 4, '$120K +': 5}

    try:
        edu_code = encoders['Education_Level'].transform([education])[0]
    except:
        edu_code = 0

    input_data = pd.DataFrame([{
        'Customer_Age': age,
        'Dependent_count': dependents,
        'Credit_Limit': credit_limit,
        'Total_Revolving_Bal': revolving_bal,
        'Total_Trans_Amt': trans_amt,
        'Total_Trans_Ct': trans_ct,
        'Avg_Utilization_Ratio': utilization,
        'Income_Encoded': income_map[income],
        'Education_Level_Code': edu_code
    }])

    # Predict
    with st.spinner("Analyzing patterns..."):
        time.sleep(0.4)
        prediction = model.predict(input_data)[0]
        probs = model.predict_proba(input_data)
        confidence = np.max(probs) * 100

    # -------------------------------------------------------------------------
    # 5. UI: RESULTS & EXPLANATION
    # -------------------------------------------------------------------------

    st.title("Prediction Analysis")
    st.markdown("<br>", unsafe_allow_html=True) # Spacer

    # --- Top Cards ---
    col1, col2 = st.columns(2)

    # Dynamic Colors - Adjusted for Dark Mode Visibility
    colors = {
        "Blue": "#33A1FD",       # Bright Blue
        "Silver": "#E0E0E0",     # Bright Silver/White
        "Gold": "#FFD700",       # Bright Gold
        "Platinum": "#E5E4E2"    # Platinum (Metallic White), NOT Black
    }
    p_color = colors.get(prediction, "#FFFFFF")

    with col1:
        st.markdown(f"""
        <div class="metric-container" style="border-top: 6px solid {p_color};">
            <div class="stat-label">Predicted Category</div>
            <div class="big-stat" style="color: {p_color};">{prediction}</div>
        </div>
        """, unsafe_allow_html=True)

    with col2:
        st.markdown(f"""
        <div class="metric-container" style="border-top: 6px solid #2ecc71;">
            <div class="stat-label">Model Confidence</div>
            <div class="big-stat" style="color: #FAFAFA;">{confidence:.1f}%</div>
        </div>
        """, unsafe_allow_html=True)

    # --- THE "WHY" SECTION ---
    st.markdown("<br><hr>", unsafe_allow_html=True)
    st.subheader(f"🔍 Insight: Why {prediction}?")
    st.write(f"Comparing this customer against the average **{prediction}** member profile.")

    st.markdown("<br>", unsafe_allow_html=True)

    c_explain_1, c_explain_2 = st.columns([1.2, 0.8])

    with c_explain_1:
        # Compare Input vs Average
        segment_avg = profiles.loc[prediction]
        radar_metrics = ['Credit_Limit', 'Total_Trans_Amt', 'Total_Trans_Ct', 'Total_Revolving_Bal']

        def normalize(val, col_name):
            max_val = raw_df[col_name].max()
            return val / max_val if max_val > 0 else 0

        input_norm = [normalize(input_data[m].values[0], m) for m in radar_metrics]
        avg_norm = [normalize(segment_avg[m], m) for m in radar_metrics]

        # --- DARK MODE CHART ---
        fig = go.Figure()

        # Customer Polygon
        fig.add_trace(go.Scatterpolar(
            r=input_norm, theta=radar_metrics, fill='toself',
            name='This Customer', line_color=p_color
        ))

        # Average Polygon
        fig.add_trace(go.Scatterpolar(
            r=avg_norm, theta=radar_metrics, fill='toself',
            name=f'Avg {prediction} User', line_color='#888888',
            line=dict(dash='dash')
        ))

        fig.update_layout(
            template='plotly_dark', # Critical for Dark Mode
            paper_bgcolor='rgba(0,0,0,0)', # Transparent background
            plot_bgcolor='rgba(0,0,0,0)',
            polar=dict(
                radialaxis=dict(visible=False),
                bgcolor='#262730' # Match card background
            ),
            showlegend=True,
            height=400,
            margin=dict(t=20, b=20, l=40, r=40),
            legend=dict(yanchor="bottom", y=-0.2, xanchor="center", x=0.5, orientation="h")
        )
        st.plotly_chart(fig, use_container_width=True)

    with c_explain_2:
        st.markdown(f"##### Key Drivers for {prediction}")

        # Logic
        explanations = []
        if prediction in ["Gold", "Platinum"]:
            if input_data['Total_Trans_Amt'].values[0] > 8000:
                explanations.append(f"**High Transaction Volume**<br>Total spend of ${input_data['Total_Trans_Amt'].values[0]:,.0f} is a strong indicator for Premium tiers.")
            if input_data['Credit_Limit'].values[0] > 15000:
                explanations.append(f"**High Credit Limit**<br>Access to ${input_data['Credit_Limit'].values[0]:,.0f} credit aligns with {prediction} profiles.")

        elif prediction == "Blue":
            if input_data['Total_Trans_Amt'].values[0] < 5000:
                explanations.append(f"**Standard Spending**<br>Transaction volume is typical for the Blue entry-tier.")
            if input_data['Credit_Limit'].values[0] < 10000:
                explanations.append(f"**Standard Credit Limit**<br>Limit fits the standard profile.")

        # Display Explanations
        if explanations:
            for exp in explanations:
                st.markdown(f"""
                <div class="highlight-box" style="margin-bottom:10px;">
                    {exp}
                </div>
                """, unsafe_allow_html=True)
        else:
            st.markdown(f"""
            <div class="highlight-box">
                This customer's profile perfectly matches the standard characteristics of the <b>{prediction}</b> segment across all key dimensions.
            </div>
            """, unsafe_allow_html=True)

else:
    # Default State (Instruction)
    st.markdown("""
    <div style='text-align: center; padding: 50px; color: #FAFAFA; background-color: #262730; border-radius: 10px; margin-top: 20px; border: 1px solid #41444C;'>
        <h2>Waiting for Input...</h2>
        <p style="font-size: 1.2rem; color: #B0B0B0;">Upload the <b>DataSet.csv</b> in the sidebar, fill out the customer form, and click <b>Predict Category</b>.</p>
    </div>
    """, unsafe_allow_html=True)